$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: 'Dashboard: 2 columns'
status: stable
group: Dashboard
description: 'A narrow region next to a wide region.'
props:
  type: object
  properties:
    reverse:
      type: boolean
      title: 'Reverse columns'
      description: 'Put the wide region first.'
slots:
  one:
    title: 'Left'
  two:
    title: 'Right'
