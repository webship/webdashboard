{#
/**
 * @file
 * Gin theme implementation of a dashboard item list.
 *
 * The whole item is a link, the way Gin draws its administration lists.
 *
 * @see webdashboard-admin-list.html.twig
 *
 * @ingroup themeable
 */
#}
{% set classes = [
  'admin-list--panel',
  'admin-list',
] %}
<dl{{ attributes.addClass(classes) }}>
  {% for item in list %}
    <div class="admin-item{% if item.image %} admin-item-with-image{% endif %}">
      {% if item.image %}
        <div class="image"><img src="{{ item.image }}" alt="" /></div>
        <div class="content">
      {% endif %}
      {# The link stays inside the term: a definition list takes only dt, dd
         and div children. The stretched link keeps the whole item clickable,
         the way Gin draws its administration lists. #}
      <dt class="admin-item__title">
        <a{{ item.link_attributes.addClass(['admin-item__link', 'admin-item__link--stretched']) }}>{{ item.title }}</a>
      </dt>
      {% if item.description %}
        <dd class="admin-item__description">
          {{ item.description }}
        </dd>
      {% endif %}
      {% if item.image %}
        </div>
      {% endif %}
    </div>
  {% endfor %}
</dl>
